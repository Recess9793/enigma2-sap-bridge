services:
  enigma2-sap-bridge:
    build: .
    container_name: enigma2-sap-bridge
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    cap_add:
      - NET_RAW
    volumes:
      - ./data:/data
